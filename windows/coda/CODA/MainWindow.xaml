<!-- -*- mode: nxml; nxml-child-indent: 2; tab-width: 4; indent-tabs-mode: nil -*- -->
<Window x:Class="CODA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:CODA"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Menu>
      <MenuItem Header="File" Grid.Row="0">
        <MenuItem Header="Open..." Click="MainWindow_FileOpen"/>
        <MenuItem Header="Save"/>
        <MenuItem Header="Save As..."/>
        <Separator/>
        <MenuItem Header="Exit"/>
      </MenuItem>
    </Menu>
    <!-- This TextBlock and the WebView2 intentionally overlap. The
         TextBlock is hidden once we open a document. -->
    <TextBlock x:Name="useFileDialogNote" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="28" Grid.Row="1">
      Use the File menu to open a document
    </TextBlock>
    <wv2:WebView2 x:Name="webView2XamlElement" Grid.Row="1">
      <wv2:WebView2.CreationProperties>
        <wv2:CoreWebView2CreationProperties BrowserExecutableFolder="" AdditionalBrowserArguments="" />
      </wv2:WebView2.CreationProperties>
    </wv2:WebView2>
  </Grid>
</Window>
